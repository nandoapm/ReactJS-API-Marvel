
import React from 'react'
import CharacterList from '../pages/characterList'

describe( 'Tests for CharacterList componenet', () => {
  it('Should add new search when insert name', () => {
    //Realizar a busca no campo search

  })
})
